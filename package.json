{
  "name": "replay-benchmarks",
  "version": "0.0.1",
  "description": "A repo to compare two different versions of Replay at the same time",
  "main": "core.ts",
  "scripts": {
    "core": "ts-node -r tsconfig-paths/register core.ts",
    "opt": "node --trace-deopt --trace-opt -r esm opt.js"
  },
  "author": "Ed Bentley",
  "license": "MIT",
  "dependencies": {
    "@types/benchmark": "^2.1.0",
    "@types/node": "^14.14.31",
    "benchmark": "^2.1.4",
    "esm": "^3.2.25",
    "ts-node": "^9.1.1",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.3"
  }
}
